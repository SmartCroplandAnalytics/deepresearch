你在规划研究任务的子范围分解。下面给出**语料中与【范围】相关、且经多样化采样的代表文献标题**（覆盖该范围的不同侧面，非仅最热门几篇）。据此把范围拆成至多 $k 个子范围，要求：
① **发散**：刻意覆盖**不同侧面**（含较少见 / 边缘但语料确有的角度），**不要都挤在最热门的几篇上**；
② **互斥互补（MECE）**：子范围之间尽量不重叠；
③ 只拆语料里确有支撑的方向，不要拆出标题里看不到的；
若语料对该范围只够一个具体角度、无需再拆，返回空数组。只输出 JSON 数组：["子范围1",...] 或 []
