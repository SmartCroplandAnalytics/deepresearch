---
name: chart
description: 绘图能力——正常对话里用 matplotlib 真的画出折线/柱状/散点等图（隔离子进程沙箱渲染，出 PNG 落工作区，直接在对话里显示）。用户说"画个图/折线图/柱状图"时用本能力。
version: 0.1
kind: capability
---

# chart 能力（沙箱绘图）

让对话 agent 在普通对话里**真的画出图**（而不是把代码丢给用户本地跑）。绘图代码在
**隔离子进程**里执行：环境白名单剥离了所有密钥与 DSN、禁止文件/网络/系统访问、用本环境
venv 的 python、Agg 无界面、30s 超时——安全地出一张 PNG 落到工作区 `figures/`，对话与右侧
暂存区都能看到、可下载。

## 工具

- `render_chart(code)`：跑一段 **matplotlib pyplot** 绘图代码，返回图片相对路径。
  - 数据**内联写进代码**（如 `years=[2019,2020,...]`、`values=[85.98,86.45,...]`）。
  - 正常 `plt.plot / plt.bar / plt.scatter / plt.title / plt.xlabel …` 即可；**中文照常写**，
    字体已配好；**无需自己存盘**（工具自动保存当前图）。
  - **禁止**：读写文件（`open`）、联网（requests/urllib）、子进程、访问环境变量/系统——命中即被拒。

## 执行纪律

1. 数据先用查库工具（如 `db_series`）拿到真值，再把这些数值**内联**进绘图代码，**不要编造**。
2. 调 `render_chart(code)`；成功后**务必**在回复里用 `![标题](figures/xxx.png)`（路径见返回值）
   把图展示出来。
3. 失败（返回里有报错）就按报错**改代码重试**，不要把原始代码丢给用户让其本地运行。
